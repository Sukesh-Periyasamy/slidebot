{
  "name": "@slidebot/renderer",
  "version": "0.0.1",
  "private": true,
  "description": "Renderer utilities for SlideBot — bounding box computation, dirty region tracking",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@slidebot/tsconfig": "workspace:*",
    "fast-check": "^4.8.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
