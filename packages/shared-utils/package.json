{
  "name": "@slidebot/shared-utils",
  "version": "0.0.1",
  "private": true,
  "description": "Shared pure utility functions for SlideBot",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@slidebot/tsconfig": "workspace:*",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  }
}
